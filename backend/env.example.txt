# Copia este archivo como .env en la carpeta backend
# Reemplaza los valores según tu configuración

# Base de datos PostgreSQL
# Formato: postgresql://usuario:contraseña@host:puerto/nombre_base_datos?schema=public
DATABASE_URL="postgresql://postgres:TU_CONTRASEÑA@localhost:5432/Rendix?schema=public"

# JWT Secret - Genera uno aleatorio y seguro
# Puedes generar uno ejecutando en PowerShell:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="tu_secret_key_super_segura_aqui_cambiala_por_algo_aleatorio"

# Puerto del servidor (opcional, ya está configurado en app.js)
PORT=3001

